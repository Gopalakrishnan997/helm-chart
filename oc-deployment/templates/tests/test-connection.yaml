apiVersion: v1
kind: Pod
metadata:
  name: "oc-deployment-test-connection"
  labels:
    {{- include "oc-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['ui-helm-app.20.127.63.130.nip.io']
  restartPolicy: Never
