apiVersion: v1
kind: Pod
metadata:
  name: "oc-deployment-test-connection"
  labels:
    {{- include "oc-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['172.30.118.50:8080']
  restartPolicy: Never
